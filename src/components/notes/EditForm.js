import React from 'react';

const EditForm = (props) => {
    console.log(props, 'cek props edit');
    return (
        <div>
            Edit form page
        </div>
    );
}

export default EditForm;
